const Actions = () => {
  return (
    <div style={{ display: "flex", gap: 10 }}>
      <button>Message</button>
      <button>Block</button>
      <button>Report</button>
    </div>
  );
};

export default Actions;
